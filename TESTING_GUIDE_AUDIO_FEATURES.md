# 🧪 دليل الاختبار السريع

## اختبار الصوت

### 1️⃣ صوت التسبيح (Tasbeeh Click)
```
الطريقة:
1. انقر على "التسبيح" في القائمة السفلية
2. اضغط الزر الكبير "+" لزيادة العداد
3. يجب أن تسمع صوت كليك خفيف

المتوقع: ✅ صوت كليك يعمل كل مرة
الفشل: ❌ بدون صوت (تحقق من volume)
```

### 2️⃣ اختبار الصوت الفوري
```
الكود المباشر في وحدة التحكم:
import { playTestSound } from './services/audioInitService';
await playTestSound();

المتوقع: ✅ صوت بيب خفيف
```

---

## اختبار المهمات اليومية

### 3️⃣ عرض المهمات
```
الطريقة:
1. انقر على "⭐ المهمات" في القائمة السفلية
2. يجب أن ترى 3 تبويبات: المهمات، الأوراد، الاختبارات

المتوقع:
✅ رؤية المهمات اليومية
✅ رؤية الأوراد المقترحة
✅ رؤية الأسئلة
✅ عرض النقاط المجموعة
```

### 4️⃣ إكمال مهمة
```
الطريقة:
1. في تبويب "المهمات"
2. اضغط على أي مهمة (مثل صلاة الفجر)
3. يجب أن تتحول إلى أخضر وتظهر علامة ✓

المتوقع: ✅ المهمة توضع علامة "مكتملة"
التحقق: افحص LocalStorage:
  localStorage.getItem('noor_completed_missions_v1')
```

### 5️⃣ تتبع النقاط
```
الطريقة:
1. أكمل عدة مهمات
2. لاحظ عداد النقاط أعلى الصفحة

المتوقع: ✅ النقاط تزداد (50 لكل مهمة)
التحقق: افحص localStorage للتأكيد
```

---

## اختبار التسميع والتصحيح

### 6️⃣ بدء التسميع
```
الطريقة:
1. انقر على "🎙️ التسميع" (من القائمة الإضافية)
2. اختر سورة من القائمة
3. اضغط "🔴 ابدأ التسجيل"

المتوقع:
✅ يبدأ العد التنازلي
✅ يسجل صوتك
⚠️ قد يطلب صلاحية الميكروفون
```

### 7️⃣ اختبار التصحيح
```
الطريقة (اختبار يدوي):
1. في وحدة التحكم:
```typescript
import tassemCorrectorService from './services/tassemCorrectorService';

// اختبر النص الصحيح
let result = tassemCorrectorService.correctTassem(
  1, // سورة الفاتحة
  1, // آية 1
  'الحمد لله رب العالمين' // النص الصحيح
);
console.log(`نص صحيح: ${result.score}%`); // يجب أن تكون قريبة من 100

// اختبر نص خاطئ
result = tassemCorrectorService.correctTassem(
  1,
  1,
  'مرحبا بك' // نص مختلف تماماً
);
console.log(`نص خاطئ: ${result.score}%`); // يجب أن تكون منخفضة
```

المتوقع:
✅ النص الصحيح = درجة عالية (90+)
✅ النص الخاطئ = درجة منخفضة (0-30)
✅ نص قريب = درجة متوسطة (50-80)
```

### 8️⃣ عرض النتائج
```
الطريقة:
1. في البطاقة، اضغط "تصحيح"
2. يجب أن يظهر modal بالنتائج

المتوقع:
✅ عرض الدرجة بنسبة مئوية
✅ رسالة feedback واضحة
✅ قائمة بالمشاكل (إن وجدت)
✅ اقتراحات للتحسين
```

### 9️⃣ الإحصائيات
```
الطريقة:
في وحدة التحكم:
```typescript
import tassemCorrectorService from './services/tassemCorrectorService';

const stats = tassemCorrectorService.getStatistics();
console.log(stats);
// {
//   totalSessions: 0,      // عدد الجلسات
//   correctSessions: 0,    // الجلسات الناجحة
//   successRate: 0,        // نسبة النجاح %
//   averageScore: 0,       // متوسط الدرجة
//   recentSessions: []     // آخر جلسات
// }
```

المتوقع: ✅ إحصائيات صحيحة تزداد مع كل جلسة
```

---

## التحقق من البيانات المحفوظة

### 🔟 التحقق من LocalStorage
```
في وحدة التحكم:

// 1. المهمات المكتملة
JSON.parse(localStorage.getItem('noor_completed_missions_v1'))
// يجب أن تراها إذا أكملت مهام

// 2. جلسات التسميع
JSON.parse(localStorage.getItem('tassem_sessions_v1'))
// يجب أن تراها إذا اختبرت التسميع

// 3. حالة التسبيح
JSON.parse(localStorage.getItem('tasbih_state_v2'))
// يجب أن تراها إذا استخدمت التسبيح
```

---

## الأخطاء الشائعة والحلول

### ❌ الصوت لا يعمل
```
الأسباب المحتملة:
1. مستوى الصوت = 0
   الحل: ارفع مستوى الصوت على الجهاز

2. تعطيل الصوت من نظام التشغيل
   الحل: تفعيل الصوت في الإعدادات

3. متصفح قديم
   الحل: استخدم Chrome, Firefox, Edge حديث

اختبر:
import { playTestSound } from './services/audioInitService';
await playTestSound();
```

### ❌ المهمات لا تظهر
```
الحل:
1. تحديث الصفحة (F5)
2. افتح وحدة التحكم وابحث عن أخطاء
3. امسح cache: Ctrl+Shift+Delete

التحقق:
console.log(localStorage.getItem('noor_completed_missions_v1'));
```

### ❌ التصحيح لا يعطي نتائج
```
الحل:
1. تأكد من أن رقم السورة صحيح (1-114)
2. تأكد من أن رقم الآية صحيح
3. جرب مع سورة الفاتحة (مضمونة موجودة)

اختبر:
const result = tassemCorrectorService.correctTassem(
  1,    // سورة الفاتحة
  1,    // أول آية
  'الحمد لله رب العالمين'
);
console.log(result);
```

---

## قائمة اختبار شاملة

```
🎵 الصوت:
[ ] الكليك في التسبيح يعمل
[ ] اختبار الصوت يعمل
[ ] لا توجد رسائل خطأ في التحكم

📋 المهمات:
[ ] الصفحة تحمل بدون أخطاء
[ ] تظهر جميع الفئات الثلاث
[ ] النقاط تُعرض بشكل صحيح
[ ] إكمال المهمة يُحفظ
[ ] النقاط تزداد صحيحاً

🎙️ التسميع:
[ ] يمكن اختيار السورة
[ ] يمكن بدء التسجيل
[ ] يمكن إيقاف التسجيل
[ ] يمكن تشغيل التسجيل المحفوظ
[ ] يمكن حذف التسجيل

✅ التصحيح:
[ ] اضغط تصحيح = يفتح modal
[ ] الدرجة تظهر بشكل صحيح
[ ] الملاحظات منطقية
[ ] الاقتراحات مفيدة
[ ] الإحصائيات تُجمع

💾 البيانات:
[ ] المهمات تُحفظ في localStorage
[ ] الجلسات تُحفظ في localStorage
[ ] البيانات تبقى بعد إعادة تحميل الصفحة
[ ] يمكن تنظيف البيانات (اختياري)
```

---

## نصائح الاختبار

1. **افتح وحدة التحكم** (F12) لرؤية أي أخطاء
2. **اختبر على متصفح حديث** (Chrome, Firefox, Edge)
3. **افعّل جميع الأصوات** على جهازك
4. **امح cache** إذا حدثت مشاكل
5. **اختبر على هاتف أيضاً** للتأكد
6. **افحص localStorage** للتحقق من الحفظ

---

**كل اختبار نجح؟ ممتاز! 🎉**
**إذا فشل شيء؟ تحقق من الأخطاء أعلاه ✅**
